#!/bin/zsh
{{ if eq .machine "linux-fedora"}}
__conda_prefix="$HOME/Programs/miniforge3" # Set your conda Location

# >>> conda initialize >>>
__conda_setup="$("$__conda_prefix/bin/conda" 'shell.bash' 'hook' 2>/dev/null)"
if [ $? -eq 0 ]; then
eval "$__conda_setup"
else
if [ -f "$__conda_prefix/etc/profile.d/conda.sh" ]; then
. "$__conda_prefix/etc/profile.d/conda.sh"
else
export PATH="$__conda_prefix/bin:$PATH"
fi
fi
unset __conda_setup

if [ -f "/home/theo/Programs/miniforge3/etc/profile.d/mamba.sh" ]; then
. "/home/theo/Programs/miniforge3/etc/profile.d/mamba.sh"
fi
# <<< conda initialize <<<

unset __conda_prefix
# unfunction load_conda
{{ end}}



{{ if eq .machine "linux-wsl"}}
# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/theo/miniforge3/bin/conda' 'shell.zsh' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/theo/miniforge3/etc/profile.d/conda.sh" ]; then
        . "/home/theo/miniforge3/etc/profile.d/conda.sh"
    else
        export PATH="/home/theo/miniforge3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<
{{ end}}
